Build a full web application called Phone Contact App. Follow these instructions exactly.

TECH STACK REQUIREMENTS

Frontend: React + TailwindCSS

Backend: Node.js + Express

Database: MongoDB (Mongoose)

API style: REST

Use clean folder structure (controllers, models, routes, services).

Use environment variables for API keys.

FEATURES TO IMPLEMENT
1. User Authentication

Email/password signup & login

JWT-based auth

Protect all CRUD routes behind auth middleware

2. Contacts (CRUD)

A Contact object has:

name (string)

phone (string)

email (string)

notes (string)
Users can:

Create a contact

View contact list

Update a contact

Delete a contact

3. Groups (CRUD)

A Group object has:

name (string)

description (string)

contacts: array of Contact IDs

schedule: array of { date, frequency }

backgroundInfo (string used for AI messages)

Users can:

Create a group

Edit a group

Delete a group

Add/remove contacts from the group

4. AI Message Generation

For each group:

Use the group’s backgroundInfo

Generate a personalized message

Display message in UI so user can edit before sending

(Implement AI call as a function stub so I can plug in an API key later.)

5. Message Sending

Support sending via:

SMS (Twilio placeholder function)

Email (SendGrid/Mailgun placeholder function)

When a message is sent:

Save a log entry

Notify the user (simple UI alert or toast)

6. Message Logs

A MessageLog object contains:

groupId

messageContent

recipients

timestamp

Create a UI page that lists all logs for the authenticated user.

7. Scheduling

Implement backend scheduling system using node-cron:

Each group can have recurring dates

On each date:

Generate AI message

Send message

Save log

Notify the user

Use placeholders for AI/SMS/email calls.

UI REQUIREMENTS

Pages to create:

Login / Signup

Dashboard

Contacts list + create/edit modal

Groups list + create/edit page

Group page where user triggers AI message generation

Message logs page

Use clean, minimal Tailwind UI.

FINAL INSTRUCTIONS

Do not invent features not listed.

Keep code modular, simple, and scalable.

Create all backend routes + controllers + models.

Create all frontend pages + components.

Make sure contact/group CRUD and logs fully work.